# Copy to .env.local for development. Never commit secrets.

# Server-side base URL used by Next.js (server components + the /api proxy)
# to reach the Django API. Not exposed to the browser bundle.
API_BASE_URL=http://localhost:8000

# Public site URL (used for canonical URLs, sitemap, robots).
SITE_URL=http://localhost:3000
